spring:
  application:
    name: urlservice
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/shortlink_url_db?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8191

management:
  endpoints:
    web:
      exposure:
        include: health,info
application:
  short-url:
    base-url: ${SHORT_URL_BASE_URL:http://localhost:8191}
    code-length: 7